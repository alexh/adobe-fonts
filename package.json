{
  "name": "adobe-fonts-skill-repo",
  "version": "0.1.0",
  "private": true,
  "description": "Publishable Adobe Fonts skill for Codex, Claude Code, and OpenCode",
  "license": "MIT",
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/afont.test.js && bash tests/smoke.sh",
    "test:unit": "node --test --test-concurrency=1 tests/afont.test.js",
    "test:smoke": "bash tests/smoke.sh"
  },
  "dependencies": {
    "playwright": "^1.51.1"
  },
  "engines": {
    "node": ">=18"
  }
}
